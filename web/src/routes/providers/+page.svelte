<script lang="ts">import ProviderManager from '$lib/ProviderManager.svelte';import ServiceManager from '$lib/ServiceManager.svelte';let tab=$state<'llm'|'tts'>('llm');</script>
<div class="mx-auto max-w-7xl"><header><div class="eyebrow">Providers & services</div><h1 class="mt-2 text-4xl font-semibold">Connections</h1><p class="muted mt-3">Application-wide model, cost, endpoint, catalogue, and readiness settings.</p></header><div class="mt-7 flex gap-2 border-b border-[var(--line)]"><button class:active={tab==='llm'} onclick={()=>tab='llm'}>LLM providers</button><button class:active={tab==='tts'} onclick={()=>tab='tts'}>TTS services</button></div><div class="mt-7">{#if tab==='llm'}<ProviderManager onback={()=>history.back()}/>{:else}<ServiceManager/>{/if}</div></div>
<style>button{border-bottom:2px solid transparent;padding:.75rem 1rem;color:var(--muted);font-size:.85rem;font-weight:700}button.active{border-color:var(--accent);color:var(--ink)}</style>
